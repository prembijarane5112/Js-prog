In the above program, the method is used to create a timer.

The method is executed at a given interval time 

(here, 2000 milliseconds).

The gives the current date and time. For example,

let d1 = new Date();

console.log(time); // Fri Aug 28 2020 09:19:40 GMT+0545 (+0545)
The method returns the number of milliseconds from midnight

of January 1, 1970 (EcmaScript epoch) to the specified date (here, current



The remaining number of day is calculated using:

 The time interval is divided by 1000 to determine the number of seconds,

i.e. timeLeft / 1000

 The time interval then is divided by 60 * 60 * 24 to determine the number of days 

remaining.

 The Math.floor() function is used to round the number to a whole number.

Similar methods are used for hours, minutes, and seconds.

Conclusion:

--------------------------------------------------------------------------------------------------------------------

--------------------------------------------------------------------------------------------------------------------

--------------------------------------------------------------------------------------------------------------------

--------------------------------------------------------------------------------------------------------------------

--------------------------------------------------------------------------------------------------------------------

The method returns the number of milliseconds from midnight

of January 1, 1970 (EcmaScript epoch) to the specified date (here, current 

date).

